services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: jbg_web
    restart: always
    user: "${UID:-0}:${GID:-0}" # Allow access to mounted volumes
    ports:
      - 80:80
